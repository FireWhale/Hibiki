<% content_for :title do %>Organizations<% end %>

<% content_for :header do %>
  <h2 class="text-center">Organizations</h2>
<% end %>

<div>
  <%= paginate @organizations %>
  <div class="well">
    <%= render 'layouts/artist_org_source_index', :collection => @organizations %>
  </div>
  <%= paginate @organizations %>
</div>

<% content_for :usersidebar do %>
<% end %>

